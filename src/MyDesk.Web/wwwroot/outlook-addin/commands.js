// Placeholder for future function-command handlers (e.g. one-click "Log this")
// so we don't have to open the task pane. Registered via commands.html in the
// manifest. Kept empty for v1 — the manifest only wires ShowTaskpane actions.
Office.onReady(function () {
  /* no-op */
});
