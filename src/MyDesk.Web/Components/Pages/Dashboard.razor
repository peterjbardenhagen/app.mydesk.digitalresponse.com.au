@page "/dashboard"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to recent activity as the main dashboard
        Navigation.NavigateTo("/activity/recent", forceLoad: true);
    }
}
